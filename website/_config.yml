title: ACP Sure Step
description: >-
  A Java-based workflow engine for orchestrating AI agent tasks using the Agent Client Protocol (ACP).
  Define and execute complex task sequences that interact with AI agents and shell commands.
baseurl: "/acp-sure-step"
url: "https://htynkn.github.io"

theme: minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag

exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
